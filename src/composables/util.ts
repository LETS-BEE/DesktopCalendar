/**
 * @deprecated Import from `stores/desktopCalendar`, `services/desktopApi`,
 * or `shared/color` directly. This facade remains during the module migration.
 */
export { useDeskCalStore, type DesktopCalStore } from '../stores/desktopCalendar'
export { desktopApi } from '../services/desktopApi'
export { convertColor } from '../shared/color'
